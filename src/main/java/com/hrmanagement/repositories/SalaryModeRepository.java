package com.hrmanagement.repositories;

import com.hrmanagement.entities.SalaryMode;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SalaryModeRepository extends JpaRepository<SalaryMode, Integer> {
}
